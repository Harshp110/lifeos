# LifeOS Architecture Docs

This folder contains the initial architecture recommendations for LifeOS.

The repository is currently a clean seed project with only `README.md` and `.gitignore` outside Git metadata. These documents therefore define the intended production architecture before application code is written.

Start with:

- [Architecture Recommendation](./architecture-recommendation.md)

