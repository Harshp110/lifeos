# Local Docker Services

The root `docker-compose.yml` defines the local PostgreSQL and Redis services used during development.

Application processes run on the host for faster frontend and backend iteration.
